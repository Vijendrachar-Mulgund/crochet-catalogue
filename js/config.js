// config.js — Charming Yarns catalogue configuration.
//
// LEAVE BLANK to run the catalogue locally (data saved only in this browser).
//
// To turn this into the SHARED ONLINE catalogue, paste your Supabase project
// details below (see SUPABASE_SETUP.md for step-by-step instructions):
//   1. supabaseUrl     — Supabase → Project Settings → API → "Project URL"
//   2. supabaseAnonKey — the same page → "Project API keys" → "anon public"
//
// These two values are safe to put in the website (they only allow what your
// security rules permit, and editing still requires logging in).
window.CATALOGUE_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
